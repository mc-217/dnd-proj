# Backend Source Notes

This directory holds the backend runtime source files for the NestJS app.
Keep feature modules isolated here so controllers, services, and types stay organized.

Current module groups:
- root app metadata (`app.*`)
- health checks (`health/`)
- character APIs (`characters/`)
